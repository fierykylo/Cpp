#include <bits/stdc++.h>
using namespace std;

int main(void)
{
    int num;
    cout << "sum of how many numbers ? \n";
    cin >> num;
    int sum;
    sum = (num * (num + 1)) / 2;
    cout << sum;
}